const authorizeTypes = {
	vk: 'vk',
	facebook: 'facebook',
	google: 'google',
	line: 'line',
	local: 'local',
};

const permissions = {

};

const roles ={
	user:'user',
	admin:'admin',
};

module.exports = {
	authorizeTypes,
	permissions,
	roles,
};